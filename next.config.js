/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com'], // Add Google's hostname for profile images
  },
};

module.exports = nextConfig; 